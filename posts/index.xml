<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Posts on Christopher Hannan</title>
        <link>https://cphannan.github.io/posts/</link>
        <description>Recent content in Posts on Christopher Hannan</description>
        <generator>Hugo -- gohugo.io</generator>
        <copyright>&lt;a href=&#34;https://creativecommons.org/licenses/by-nc/4.0/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;CC BY-NC 4.0&lt;/a&gt;</copyright>
        <lastBuildDate>Sat, 06 Jun 2020 00:00:00 +0000</lastBuildDate>
        <atom:link href="https://cphannan.github.io/posts/index.xml" rel="self" type="application/rss+xml" />
        
        <item>
            <title>Ansible and Cisco DevNet: Part 2. Writing a simple playbook</title>
            <link>https://cphannan.github.io/posts/2020/06/ansible-and-cisco-devnet-part-2.-writing-a-simple-playbook/</link>
            <pubDate>Sat, 06 Jun 2020 00:00:00 +0000</pubDate>
            
            <guid>https://cphannan.github.io/posts/2020/06/ansible-and-cisco-devnet-part-2.-writing-a-simple-playbook/</guid>
            <description>Table of Contents  Objectives Prerequisites Guide Takeaways  Objectives  Write a simple playbook to retrieve facts from network devices Test the new playbook against Cisco&amp;rsquo;s DevNet sandboxes  An Ansible playbook is written in YAML and orchestrates the playbook &amp;lsquo;run&amp;rsquo;. The top section specifies what devices are targeted for this run and the tasks list is an ordered set of instructions to execute on each device.
We will now write a playbook that utilises the Ansible ios_facts module, used to collect a base set of facts from Cisco IOS devices.</description>
            <content type="html"><![CDATA[<h2 id="table-of-contents">Table of Contents</h2>
<ul>
<li><a href="#objectives">Objectives</a></li>
<li><a href="#prerequisites">Prerequisites</a></li>
<li><a href="#guide">Guide</a></li>
<li><a href="#takeaways">Takeaways</a></li>
</ul>
<h2 id="objectives">Objectives</h2>
<ul>
<li>Write a simple playbook to retrieve facts from network devices</li>
<li>Test the new playbook against Cisco&rsquo;s DevNet sandboxes</li>
</ul>
<p>An Ansible playbook is written in YAML and orchestrates the playbook &lsquo;run&rsquo;. The top section specifies what devices are targeted for this run and the tasks list is an ordered set of instructions to execute on each device.</p>
<p>We will now write a playbook that utilises the Ansible <a href="https://docs.ansible.com/ansible/latest/modules/ios_facts_module.html" title="ios_facts">ios_facts</a> module, used to collect a base set of facts from Cisco IOS devices.</p>
<h2 id="prerequisites">Prerequisites</h2>
<blockquote>
<p><strong>Note:</strong> This guide assumes that you have already worked through the Getting Started steps covered in <a href="/posts/2020/04/ansible-and-cisco-devnet-part-1.-getting-started/">Part 1</a>.</p>
</blockquote>
<h2 id="guide">Guide</h2>
<h3 id="step-1-create-a-playbook">Step 1: Create a playbook</h3>
<p>In the root of the <code>netauto/</code> directory, create a new file named <code>play_gather-facts.yml</code>.</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ touch play_gather-facts.yml
</code></pre></div><p>YAML accepts single-line comments and I have included comments in this playbook to break down what each line is doing. These comments will be omitted when Ansible executes the playbook.</p>
<p>Use nano or a text-editor of your choice to paste in the below information:</p>
<div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">                                                  </span><span class="c">## signals the start of a YAML file</span><span class="w">
</span><span class="w">
</span><span class="w"></span>- <span class="k">name</span><span class="p">:</span><span class="w"> </span>Play<span class="w"> </span>- IOS<span class="w"> </span>audit<span class="w"> </span>report<span class="w">                      </span><span class="c">## name for the playbook, included in the output of a playbook run</span><span class="w">
</span><span class="w">  </span><span class="k">hosts</span><span class="p">:</span><span class="w"> </span>ios<span class="w">                                         </span><span class="c">## list of one or more inventory groups, separated by colons</span><span class="w">
</span><span class="w">  </span><span class="k">connection</span><span class="p">:</span><span class="w"> </span>network_cli<span class="w">                            </span><span class="c">## plugin for a CLI connection to network devices</span><span class="w">
</span><span class="w">  </span><span class="k">gather_facts</span><span class="p">:</span><span class="w"> </span>no<span class="w">                                   </span><span class="c">## &#39;no&#39; is required for Ansible versions prior to 2.9</span><span class="w">
</span><span class="w">
</span><span class="w">  </span><span class="k">tasks</span><span class="p">:</span><span class="w">                                             </span><span class="c">## start of the tasks list, executed from top to bottom on each network device in the hosts list</span><span class="w">
</span><span class="w">
</span><span class="w">  </span>- <span class="k">name</span><span class="p">:</span><span class="w"> </span>Task<span class="w"> </span><span class="m">1</span><span class="w"> </span>- Gathering<span class="w"> </span>IOS<span class="w"> </span>facts<span class="w">               </span><span class="c">## name for task 1, included in the output of a playbook run</span><span class="w">
</span><span class="w">    </span><span class="k">ios_facts</span><span class="p">:</span><span class="w">                                       </span><span class="c">## ios_facts is the Ansible module we are invoking</span><span class="w">
</span><span class="w">    </span><span class="k">register</span><span class="p">:</span><span class="w"> </span>facts<span class="w">                                  </span><span class="c">## capturing the output from the task to the variable &#39;facts&#39;</span><span class="w">
</span><span class="w">
</span><span class="w">  </span>- <span class="k">debug</span><span class="p">:</span><span class="w"> </span>var=facts<span class="w">                                 </span><span class="c">## debug is the Ansible module we are invoking for task 2, which will print the return information from the task 1</span><span class="w">
</span></code></pre></div><h3 id="step-2-run-the-playbook">Step 2: Run the playbook</h3>
<p>Our playbook is now ready to run and the command below tells Ansible that we want to run the playbook <code>play_gather-facts.yml</code> against the <code>/dev</code> inventory. Because we have listed <code>ios</code> as the target hosts list in the playbook, only the <code>[ios]</code> list from our <code>/dev</code> inventory file will be targeted; specifically, the DevNet IOS sandbox device <code>ios-xe-mgmt.cisco.com</code>.</p>
<p>Issue the Ansible command below:</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ ansible-playbook play_gather-facts.yml -i inventories/dev/
</code></pre></div><hr>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">chhannan@ubuntu:~/netauto$ ansible-playbook play_gather-facts.yml -i inventories/dev/

PLAY <span class="o">[</span>Play - IOS audit report<span class="o">]</span> ************************************************************************************************************************************************************************************

TASK <span class="o">[</span>Task <span class="m">1</span> - Gathering IOS facts<span class="o">]</span> *******************************************************************************************************************************************************************************
ok: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span>

TASK <span class="o">[</span>debug<span class="o">]</span> ******************************************************************************************************************************************************************************************************
ok: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span> <span class="o">=</span>&gt; 
  facts:
    ansible_facts:
      ansible_net_all_ipv4_addresses:
      - 9.9.9.9
      - 1.1.1.4
      - 1.2.3.4
      - 10.10.1.5
      - 13.13.13.13
      - 89.89.89.89
      .
      .
      &lt;output truncated <span class="k">for</span> readability&gt;
      .
      .
      ansible_net_hostname: csr1000v
      ansible_net_image: bootflash:packages.conf
      ansible_net_iostype: IOS-XE
      ansible_net_memfree_mb: <span class="m">2068652</span>
      ansible_net_memtotal_mb: <span class="m">2392443</span>
      ansible_net_model: CSR1000V
      ansible_net_neighbors: <span class="o">{}</span>
      ansible_net_python_version: 2.7.17
      ansible_net_serialnum: 98BH7LU5M3H
      ansible_net_system: ios
      ansible_net_version: 16.09.03
      .
      .
      &lt;output truncated <span class="k">for</span> readability&gt;

PLAY RECAP ********************************************************************************************************************************************************************************************************
ios-xe-mgmt.cisco.com      : <span class="nv">ok</span><span class="o">=</span><span class="m">2</span>    <span class="nv">changed</span><span class="o">=</span><span class="m">0</span>    <span class="nv">unreachable</span><span class="o">=</span><span class="m">0</span>    <span class="nv">failed</span><span class="o">=</span><span class="m">0</span>    <span class="nv">skipped</span><span class="o">=</span><span class="m">0</span>    <span class="nv">rescued</span><span class="o">=</span><span class="m">0</span>    <span class="nv">ignored</span><span class="o">=</span><span class="m">0</span>
</code></pre></div><p>The Play Recap shows that this playbook ran successfully and made no changes to the end device(s), it simply used the <em>ios_facts</em> module to retrieve information in key-value pairs.</p>
<ul>
<li><strong>Task 1:</strong> used the <em>ios_facts</em> module to gather a base set of facts from each device, registering the output to the variable <em>&ldquo;facts&rdquo;</em>.</li>
<li><strong>Task 2:</strong> (debug:) a temporary information gathering task that used the <a href="https://docs.ansible.com/ansible/latest/modules/debug_module.html" title="debug">debug</a> module to print the <em>&ldquo;facts&rdquo;</em> variable registered in Task 1.</li>
</ul>
<p>This information will be used in the next step to generate a report.</p>
<h3 id="step-3-add-reporting-tasks-to-the-playbook">Step 3: Add reporting tasks to the playbook</h3>
<p>We will now edit the playbook, removing the debug task and creating new tasks that:</p>
<ul>
<li>Create a folder to store our device reports.</li>
<li>Use a Jinja2 template to dynamically generate a report file for each device.</li>
<li>Consolidate each individual device report into a single report.</li>
</ul>
<p>Use nano or a text-editor of your choice to update the playbook <code>play_gather-facts.yml</code> with the below tasks:</p>
<div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">                                                  </span><span class="c">## signals the start of a YAML file</span><span class="w">
</span><span class="w">
</span><span class="w"></span>- <span class="k">name</span><span class="p">:</span><span class="w"> </span>Play<span class="w"> </span>- IOS<span class="w"> </span>audit<span class="w"> </span>report<span class="w">                      </span><span class="c">## name for the playbook, included in the output of a playbook run</span><span class="w">
</span><span class="w">  </span><span class="k">hosts</span><span class="p">:</span><span class="w"> </span>ios<span class="w">                                         </span><span class="c">## list of one or more inventory groups, separated by colons</span><span class="w">
</span><span class="w">  </span><span class="k">connection</span><span class="p">:</span><span class="w"> </span>network_cli<span class="w">                            </span><span class="c">## plugin for a CLI connection to network devices</span><span class="w">
</span><span class="w">  </span><span class="k">gather_facts</span><span class="p">:</span><span class="w"> </span>no<span class="w">                                   </span><span class="c">## &#39;no&#39; is required for Ansible versions prior to 2.9</span><span class="w">
</span><span class="w">
</span><span class="w">  </span><span class="k">tasks</span><span class="p">:</span><span class="w">                                             </span><span class="c">## start of the tasks list, executed from top to bottom on each network device in the hosts list</span><span class="w">
</span><span class="w">
</span><span class="w">  </span>- <span class="k">name</span><span class="p">:</span><span class="w"> </span>Task<span class="w"> </span><span class="m">1</span><span class="w"> </span>- Gathering<span class="w"> </span>IOS<span class="w"> </span>facts<span class="w">               </span><span class="c">## name for task 1, included in the output of a playbook run</span><span class="w">
</span><span class="w">    </span><span class="k">ios_facts</span><span class="p">:</span><span class="w">                                       </span><span class="c">## ios_facts is the Ansible module we are invoking</span><span class="w">
</span><span class="w">
</span><span class="w">  </span>- <span class="k">name</span><span class="p">:</span><span class="w"> </span>Task<span class="w"> </span><span class="m">2</span><span class="w"> </span>- Creating<span class="w"> </span>a<span class="w"> </span>reports<span class="w"> </span>folder<span class="w"> </span>(if<span class="w"> </span>not<span class="w"> </span>already<span class="w"> </span>present)<span class="w">
</span><span class="w">    </span><span class="k">file</span><span class="p">:</span><span class="w">                                            </span><span class="c">## file is the Ansible module we are invoking</span><span class="w">
</span><span class="w">      </span><span class="k">name</span><span class="p">:</span><span class="w"> </span>reports<span class="w">                                  </span><span class="c">## the name to give the new directory</span><span class="w">
</span><span class="w">      </span><span class="k">state</span><span class="p">:</span><span class="w"> </span>directory<span class="w">                               </span><span class="c">## parameter of the file module, creates a directory</span><span class="w">
</span><span class="w">    </span><span class="k">run_once</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">                                   </span><span class="c">## instructs Ansible to only run this task once for the batch of hosts targeted in the playbook</span><span class="w">
</span><span class="w">
</span><span class="w">  </span>- <span class="k">name</span><span class="p">:</span><span class="w"> </span>Task<span class="w"> </span><span class="m">3</span><span class="w"> </span>- Generating<span class="w"> </span>the<span class="w"> </span>device<span class="w"> </span>reports<span class="w">
</span><span class="w">    </span><span class="k">template</span><span class="p">:</span><span class="w">                                        </span><span class="c">## template is the Ansible module we are invoking</span><span class="w">
</span><span class="w">      </span><span class="k">src</span><span class="p">:</span><span class="w"> </span>ios_report.j2<span class="w">                             </span><span class="c">## parameter of the template module, path to the Jinja2 template on our Ansible control node</span><span class="w">
</span><span class="w">      </span><span class="k">dest</span><span class="p">:</span><span class="w"> </span>reports/{{<span class="w"> </span>inventory_hostname<span class="w"> </span>}}.md<span class="w">      </span><span class="c">## parameter of the template module, location to render the Jinja2 template</span><span class="w">
</span><span class="w">  
</span><span class="w">  </span>- <span class="k">name</span><span class="p">:</span><span class="w"> </span>Task<span class="w"> </span><span class="m">4</span><span class="w"> </span>- Consolidating<span class="w"> </span>the<span class="w"> </span>device<span class="w"> </span>reports<span class="w">
</span><span class="w">    </span><span class="k">assemble</span><span class="p">:</span><span class="w">                                        </span><span class="c">## assemble is the Ansible module we are invoking</span><span class="w">
</span><span class="w">      </span><span class="k">src</span><span class="p">:</span><span class="w"> </span>reports/<span class="w">                                  </span><span class="c">## parameter of the assemble module, path to the directory containing the individual device reports</span><span class="w">
</span><span class="w">      </span><span class="k">dest</span><span class="p">:</span><span class="w"> </span>reports/ios_network_report.md<span class="w">            </span><span class="c">## parameter of the assemble module, the file to create using the concatenation of the individual device reports</span><span class="w">
</span><span class="w">    </span><span class="k">delegate_to</span><span class="p">:</span><span class="w"> </span>localhost<span class="w">                           </span><span class="c">## runs this task on our Ansible control node</span><span class="w">
</span><span class="w">    </span><span class="k">run_once</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></code></pre></div><p>If we were to run this playbook now it would fail at task 3 as we have not yet created the Jinja2 template referenced in that task <em>(ios_report.j2)</em>.</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">chhannan@ubuntu:~/netauto$ ansible-playbook play_gather-facts.yml -i inventories/dev/

PLAY <span class="o">[</span>Play - IOS audit report<span class="o">]</span> ************************************************************************************************************************************************************************************

TASK <span class="o">[</span>Task <span class="m">1</span> - Gathering IOS facts<span class="o">]</span> *******************************************************************************************************************************************************************************
ok: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span>

TASK <span class="o">[</span>Task <span class="m">2</span> - Creating a reports folder <span class="o">(</span><span class="k">if</span> not already present<span class="o">)]</span> ************************************************************************************************************************************************
changed: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span>

TASK <span class="o">[</span>Task <span class="m">3</span> - Generating the device reports<span class="o">]</span> *********************************************************************************************************************************************************************
fatal: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span>: FAILED! <span class="o">=</span>&gt; <span class="nv">changed</span><span class="o">=</span><span class="nb">false</span> 
  msg: <span class="p">|</span>-
    Could not find or access <span class="s1">&#39;ios_report.j2&#39;</span>
    Searched in:
            /netauto/templates/ios_report.j2
            /netauto/ios_report.j2
            /netauto/templates/ios_report.j2
            /netauto/ios_report.j2 on the Ansible Controller.
    If you are using a module and expect the file to exist on the remote, see the remote_src option
	to retry, use: --limit @/netauto/play_gather-facts.retry

PLAY RECAP ********************************************************************************************************************************************************************************************************
ios-xe-mgmt.cisco.com      : <span class="nv">ok</span><span class="o">=</span><span class="m">2</span>    <span class="nv">changed</span><span class="o">=</span><span class="m">1</span>    <span class="nv">unreachable</span><span class="o">=</span><span class="m">0</span>    <span class="nv">failed</span><span class="o">=</span><span class="m">1</span>    <span class="nv">skipped</span><span class="o">=</span><span class="m">0</span>    <span class="nv">rescued</span><span class="o">=</span><span class="m">0</span>    <span class="nv">ignored</span><span class="o">=</span><span class="m">0</span>

chhannan@ubuntu:~/netauto$ 
</code></pre></div><h3 id="step-4-create-the-jinja2-template-referenced-in-task-3">Step 4: Create the Jinja2 template referenced in Task 3</h3>
<p>We will now use the key-value pairs retrieved in Step 2 of this guide to create a template of how we want our report file to look and what information it should hold about each device targeted in the playbook run. The template will use the keys from facts gathered by the <em>ios_facts</em> module as variables to dynamically render a device report.</p>
<p>In the root of the <code>netauto/</code> directory, create a new file named <code>ios_report.j2</code>.</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ touch ios_report.j2
</code></pre></div><p>Use nano or a text-editor of your choice to paste in the below template:</p>
<pre><code class="language-jinja2" data-lang="jinja2">---
**{{ inventory_hostname.upper() }}**

Model: {{ ansible_net_model }}

Serial Number: {{ ansible_net_serialnum }}

Software Type: {{ ansible_net_iostype }}

Version: {{ ansible_net_version }}

Image: {{ ansible_net_image }}
</code></pre><blockquote>
<p><strong>Note:</strong> we manipulate the {{ inventory_hostname }} variable by applying the .upper() filter to make the rendered output of this variable uppercase.</p>
</blockquote>
<h3 id="step-5-run-the-updated-playbook">Step 5: Run the updated playbook</h3>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">chhannan@ubuntu:~/netauto$ ansible-playbook play_gather-facts.yml -i inventories/dev/

PLAY <span class="o">[</span>Play - IOS audit report<span class="o">]</span> ************************************************************************************************************************************************************************************

TASK <span class="o">[</span>Task <span class="m">1</span> - Gathering IOS facts<span class="o">]</span> *******************************************************************************************************************************************************************************
ok: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span>

TASK <span class="o">[</span>Task <span class="m">2</span> - Creating a reports folder <span class="o">(</span><span class="k">if</span> not already present<span class="o">)]</span> ************************************************************************************************************************************************
changed: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span>

TASK <span class="o">[</span>Task <span class="m">3</span> - Generating the device reports<span class="o">]</span> *********************************************************************************************************************************************************************
changed: <span class="o">[</span>ios-xe-mgmt.cisco.com<span class="o">]</span>

TASK <span class="o">[</span>Task <span class="m">4</span> - Consolidating the device reports<span class="o">]</span> ******************************************************************************************************************************************************************
changed: <span class="o">[</span>ios-xe-mgmt.cisco.com -&gt; localhost<span class="o">]</span>

PLAY RECAP ********************************************************************************************************************************************************************************************************
ios-xe-mgmt.cisco.com      : <span class="nv">ok</span><span class="o">=</span><span class="m">4</span>    <span class="nv">changed</span><span class="o">=</span><span class="m">3</span>    <span class="nv">unreachable</span><span class="o">=</span><span class="m">0</span>    <span class="nv">failed</span><span class="o">=</span><span class="m">0</span>    <span class="nv">skipped</span><span class="o">=</span><span class="m">0</span>    <span class="nv">rescued</span><span class="o">=</span><span class="m">0</span>    <span class="nv">ignored</span><span class="o">=</span><span class="m">0</span>
</code></pre></div><p>The Play Recap shows that this playbook ran successfully and 3 of the 4 tasks registered changes:</p>
<ul>
<li><strong>Task 1:</strong> no changes -  used the <em>ios_facts</em> module to gather a base set of facts from each device.</li>
<li><strong>Task 2:</strong> changed - created a <code>reports/</code> directory. Subsequent runs of this playbook would register no changes for this task.</li>
<li><strong>Task 3:</strong> changed - used the Jinja2 template to render device reports in the <code>/reports</code> directory. Subsequent runs of this playbook would register no changes for this task until we either:
<ul>
<li>modified the template</li>
<li>updated our target inventory to include new hosts</li>
</ul>
</li>
<li><strong>Task 4:</strong> changed - creates a new &ldquo;master&rdquo; report that concatenates all of the device reports into a single report file. Subsequent runs of this playbook would register no changes for this task until we either:
<ul>
<li>modified the template</li>
<li>updated our target inventory to include new hosts</li>
</ul>
</li>
</ul>
<blockquote>
<p><strong>Note:</strong> In this example we only target a single IOS device so the individual report and the &ldquo;master&rdquo; report will be identical. If we were to add more devices to the <code>[ios]</code> group in our <code>/dev</code> inventory, the report generated in Task 4 (<em>ios_network_report.md</em>) would contain every device&rsquo;s individual report.</p>
</blockquote>
<h3 id="step-6-view-the-rendered-reports">Step 6: View the rendered reports</h3>
<p>From the root of the <code>netauto/</code> directory we should now see a <code>reports/</code> directory that contains the two report files:</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">chhannan@ubuntu:~/netauto$ tree 
.
├── ansible.cfg
├── inventories
│   └── dev
│       ├── group_vars
│       │   ├── aci.yml
│       │   ├── iosxr.yml
│       │   ├── ios.yml
│       │   └── nxos.yml
│       ├── hosts
│       └── host_vars
│           ├── ios-xe-mgmt.cisco.com
│           │   └── ios-xe-mgmt.cisco.com.yml
│           ├── sandboxapicdc.cisco.com
│           │   └── sandboxapicdc.cisco.com.yml
│           ├── sbx-iosxr-mgmt.cisco.com
│           │   └── sbx-iosxr-mgmt.cisco.com.yml
│           └── sbx-nxos-mgmt.cisco.com
│               └── sbx-nxos-mgmt.cisco.com.yml
├── ios_report.j2                                  <span class="c1">## jinja2 template created in step 4</span>
├── play_gather-facts.yml                          <span class="c1">## playbook created in step 1</span>
└── reports                                        <span class="c1">## reports directory created in task 2 of playbook</span>
    ├── ios_network_report.md                      <span class="c1">## device reports created in task 3 of playbook</span>
    └── ios-xe-mgmt.cisco.com.md                   <span class="c1">## master report created in task 4 of playbook</span>

<span class="m">9</span> directories, <span class="m">14</span> files
</code></pre></div><p>And the rendered Markdown reports would look like this:</p>
<hr>
<p><strong>IOS-XE-MGMT.CISCO.COM</strong></p>
<p>Model: CSR1000V</p>
<p>Serial Number: 98BH7LU5M3H</p>
<p>Software Type: IOS-XE</p>
<p>Version: 16.09.03</p>
<p>Image: bootflash:packages.conf</p>
<h2 id="takeaways">Takeaways</h2>
<p>This guide and the playbook we created should hopefully highlight the power of Ansible and Jinja2 templates for quickly generating inventory reports.</p>
<p>The Jinja2 template we created and the file extension we used in Tasks 3 and 4 resulted in our reports being generated in Markdown. This could easily be changed to output our reports to .csv by changing the file extensions in tasks 3 and 4 to .csv and amending the Jinja2 template to look like this:</p>
<p><strong>ios_report.j2</strong></p>
<pre><code class="language-jinja2" data-lang="jinja2">Hostname,Model,Serial Number,Software Type,Version,Image
{{ inventory_hostname }},{{ ansible_net_model }},{{ ansible_net_serialnum }},{{ ansible_net_iostype }},{{ ansible_net_version }},{{ ansible_net_image }}
</code></pre><p>Future improvements to this playbook could include creating a dedicated &ldquo;reports&rdquo; role and utilising other Ansible OS modules such as <a href="https://docs.ansible.com/ansible/latest/modules/iosxr_facts_module.html" title="iosxr_facts">iosxr_facts</a> and <a href="https://docs.ansible.com/ansible/latest/modules/nxos_facts_module.html" title="nxos_facts">nxos_facts</a> to generate reports for other network devices types.</p>
<p>Sourcecode for this guide can be found here: <a href="https://github.com/cphannan/netauto">https://github.com/cphannan/netauto</a></p>
]]></content>
        </item>
        
        <item>
            <title>Ansible and Cisco DevNet: Part 1. Getting Started</title>
            <link>https://cphannan.github.io/posts/2020/04/ansible-and-cisco-devnet-part-1.-getting-started/</link>
            <pubDate>Sat, 18 Apr 2020 00:00:00 +0000</pubDate>
            
            <guid>https://cphannan.github.io/posts/2020/04/ansible-and-cisco-devnet-part-1.-getting-started/</guid>
            <description>Table of Contents  Objectives Prerequisites Guide Next Steps  Objectives  Install and verify the default Ansible settings Create a new Ansible environment  Prerequisites  Note: This guide assumes that you already have a control node that can run Ansible, with Python installed and access to the internet.
 Guide Step 1: Install Ansible Listed below are the installation steps for both Ubuntu and macOS. For other distributions, please follow Ansible&amp;rsquo;s Installation Guide.</description>
            <content type="html"><![CDATA[<h2 id="table-of-contents">Table of Contents</h2>
<ul>
<li><a href="#objectives">Objectives</a></li>
<li><a href="#prerequisites">Prerequisites</a></li>
<li><a href="#guide">Guide</a></li>
<li><a href="#next-steps">Next Steps</a></li>
</ul>
<h2 id="objectives">Objectives</h2>
<ul>
<li>Install and verify the default Ansible settings</li>
<li>Create a new Ansible environment</li>
</ul>
<h2 id="prerequisites">Prerequisites</h2>
<blockquote>
<p><strong>Note:</strong> This guide assumes that you already have a control node that can run Ansible, with Python installed and access to the internet.</p>
</blockquote>
<h2 id="guide">Guide</h2>
<h3 id="step-1-install-ansible">Step 1: Install Ansible</h3>
<p>Listed below are the installation steps for both Ubuntu and macOS. For other distributions, please follow Ansible&rsquo;s <a href="https://docs.ansible.com/ansible/latest/installation_guide/intro_installation.html" title="Installing Ansible">Installation Guide</a>.</p>
<p><strong>Ubuntu 18.04</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">sudo apt update
sudo apt install software-properties-common
sudo apt-add-repository --yes --update ppa:ansible/ansible
sudo apt install ansible
</code></pre></div><p><strong>macOS</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">sudo easy_install pip
sudo pip install ansible
</code></pre></div><h3 id="step-2-verify-the-ansible-install">Step 2: Verify the Ansible Install</h3>
<p>When Ansible has installed, verify the default configuration by running the <code>ansible --version</code> command.</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~$ ansible --version
ansible 2.8.6
  config <span class="nv">file</span> <span class="o">=</span> /etc/ansible/ansible.cfg
  configured module search <span class="nv">path</span> <span class="o">=</span> <span class="o">[</span><span class="s1">&#39;/home/cphannan/.ansible/plugins/modules&#39;</span>, <span class="s1">&#39;/usr/share/ansible/plugins/modules&#39;</span><span class="o">]</span>
  ansible python module <span class="nv">location</span> <span class="o">=</span> /usr/local/lib/python3.6/dist-packages/ansible
  executable <span class="nv">location</span> <span class="o">=</span> /usr/local/bin/ansible
  python <span class="nv">version</span> <span class="o">=</span> 3.6.9 <span class="o">(</span>default, Apr <span class="m">18</span> 2020, 01:56:04<span class="o">)</span> <span class="o">[</span>GCC 8.4.0<span class="o">]</span>
</code></pre></div><p>The output shows that for my installation of Ansible:</p>
<ul>
<li>The version I am running is 2.8.6</li>
<li>The default configuration file is <strong>/etc/ansible/ansible.cfg</strong></li>
<li>The version of Python used is 3.6.9</li>
</ul>
<p>View the contents of the default <strong>ansible.cfg</strong> file.</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~$ cat /etc/ansible/ansible.cfg
<span class="o">[</span>defaults<span class="o">]</span>
<span class="c1">#inventory       = /etc/ansible/hosts</span>
<span class="c1">#library         = ~/.ansible/plugins/modules:/usr/share/ansible/plugins/modules</span>
<span class="c1">#module_utils    = ~/.ansible/plugins/module_utils:/usr/share/ansible/plugins/module_utils</span>
<span class="c1">#remote_tmp      = ~/.ansible/tmp</span>
<span class="c1">#local_tmp       = ~/.ansible/tmp</span>
<span class="c1">#forks           = 5</span>
<span class="c1">#poll_interval   = 0.001</span>
<span class="c1">#ask_pass        = False</span>
<span class="c1">#transport       = smart</span>
</code></pre></div><p>The output shows that for the default Ansible configuration:</p>
<ul>
<li>The inventory it will use is <strong>/etc/ansible/hosts</strong></li>
</ul>
<p>The source of the <strong>ansible.cfg</strong> file can be in several places and Ansible will search for it in the following order, using the first file found:</p>
<ul>
<li><code>ANSIBLE_CONFIG</code> (environment variable if set)</li>
<li><code>ansible.cfg</code> (in the current directory)</li>
<li><code>~/.ansible.cfg</code> (in the home directory)</li>
<li><code>/etc/ansible/ansible.cfg</code></li>
</ul>
<blockquote>
<p><strong>Source:</strong> <a href="https://docs.ansible.com/ansible/latest/reference_appendices/config.html">https://docs.ansible.com/ansible/latest/reference_appendices/config.html</a></p>
</blockquote>
<p>We will now create a new directory for the Ansible environment and then create a custom <strong>ansible.cfg</strong> file that will override the defaults above.</p>
<h3 id="step-3-build-the-base-directory-layout">Step 3: Build the base directory layout</h3>
<p>This guide will just focus on populating the <strong>dev/</strong> inventory but the Ansible directory we create follows the alternative layout from Ansible&rsquo;s <a href="https://docs.ansible.com/ansible/latest/user_guide/playbooks_best_practices.html" title="Ansible Best Practices">best practice</a> for directory layouts. We will have a separate directory for our production and development inventories, each with their own respective group and host var files.</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~$ mkdir netauto
cphannan@ubuntu:~$ <span class="nb">cd</span> netauto
cphannan@ubuntu:~/netauto$
cphannan@ubuntu:~/netauto$ mkdir inventories
cphannan@ubuntu:~/netauto$ mkdir inventories/dev
cphannan@ubuntu:~/netauto$ mkdir inventories/dev/group_vars
cphannan@ubuntu:~/netauto$ mkdir inventories/dev/host_vars
cphannan@ubuntu:~/netauto$ mkdir roles
</code></pre></div><h3 id="step-4-create-a-custom-ansiblecfg-file">Step 4: Create a custom ansible.cfg file</h3>
<p>Use <code>nano</code> to create the new <strong>ansible.cfg</strong> file and paste in the below parameters:</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ nano ansible.cfg
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-ini" data-lang="ini"><span class="k">[defaults]</span>
<span class="na">stdout_callback</span> <span class="o">=</span> <span class="s">yaml</span>
<span class="na">timeout</span> <span class="o">=</span> <span class="s">60</span>
<span class="na">deprecation_warnings</span> <span class="o">=</span> <span class="s">False</span>
<span class="na">host_key_checking</span> <span class="o">=</span> <span class="s">False</span>
<span class="na">retry_files_enabled</span> <span class="o">=</span> <span class="s">True</span>

<span class="k">[persistent_connection]</span>
<span class="na">connect_timeout</span> <span class="o">=</span> <span class="s">60</span>
<span class="na">command_timeout</span> <span class="o">=</span> <span class="s">60</span>
</code></pre></div><h3 id="step-5-add-the-cisco-devnet-sandboxes-to-the-dev-inventory">Step 5: Add the Cisco DevNet sandboxes to the dev/ inventory</h3>
<p><a href="https://developer.cisco.com/site/sandbox/" title="Cisco DevNet">Cisco DevNet</a> have always-on sandboxes for Cisco IOS, IOS-XR, NX-OS and ACI which we will add to the <strong>dev/</strong> inventory. Providing our control node can access the devices via SSH, we could add our own lab equipment (virtual or physical) to the same inventory for testing network automation against.</p>
<p>In the example below, the <strong>dev/</strong> inventory file defines the groups <code>ios</code>, <code>iosxr</code>, <code>nxos</code>, <code>aci</code> and a &ldquo;group of groups&rdquo; called <code>always_on</code>.</p>
<p>Use <code>nano</code> to create the new <strong>hosts</strong> file in <strong>inventories/dev/</strong>.</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ nano inventories/dev/hosts
</code></pre></div><p>Paste in the below information for the Cisco DevNet hosts:</p>
<div class="highlight"><pre class="chroma"><code class="language-ini" data-lang="ini"><span class="k">[always_on]</span>
<span class="na">ios-xe-mgmt.cisco.com</span>
<span class="na">sbx-iosxr-mgmt.cisco.com</span>
<span class="na">sbx-nxos-mgmt.cisco.com</span>


<span class="k">[ios]</span>
<span class="na">ios-xe-mgmt.cisco.com ansible_host</span><span class="o">=</span><span class="s">64.103.37.51</span>


<span class="k">[iosxr]</span>
<span class="na">sbx-iosxr-mgmt.cisco.com ansible_host</span><span class="o">=</span><span class="s">64.103.37.3</span>


<span class="k">[nxos]</span>
<span class="na">sbx-nxos-mgmt.cisco.com ansible_host</span><span class="o">=</span><span class="s">64.103.37.14</span>


<span class="k">[aci]</span>
<span class="na">sandboxapicdc.cisco.com</span>
</code></pre></div><h3 id="step-6-populate-the-group-variables-for-the-dev-inventory">Step 6: Populate the group variables for the dev/ inventory</h3>
<p>Group variables are used to apply shared variables to hosts in the same inventory group. Create the new &ldquo;group_var&rdquo; files and paste in the parameters.</p>
<p><strong>For the IOS group:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ nano inventories/dev/group_vars/ios.yml 
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_network_os</span><span class="p">:</span><span class="w"> </span>ios<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_become</span><span class="p">:</span><span class="w"> </span>yes<span class="w">
</span><span class="w"></span><span class="k">ansible_become_method</span><span class="p">:</span><span class="w"> </span>enable<span class="w">
</span></code></pre></div><p><strong>For the IOS-XR group:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ nano inventories/dev/group_vars/iosxr.yml 
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_network_os</span><span class="p">:</span><span class="w"> </span>iosxr<span class="w">
</span></code></pre></div><p><strong>For the NX-OS group:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ nano inventories/dev/group_vars/nxos.yml 
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_network_os</span><span class="p">:</span><span class="w"> </span>nxos<span class="w">
</span></code></pre></div><p><strong>For the ACI group:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ nano inventories/dev/group_vars/aci.yml 
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_network_os</span><span class="p">:</span><span class="w"> </span>aci<span class="w">
</span></code></pre></div><h3 id="step-7-populate-the-host-variables-for-the-dev-inventory">Step 7: Populate the host variables for the dev/ inventory</h3>
<p>Host variables are used to assign variables to a single host. As each Cisco DevNet sandbox has its own specific connection parameters and login details, we will create host specific folders and variable files for each of the sandboxes.</p>
<blockquote>
<p><strong>Note:</strong> These credentials for the Cisco DevNet sandboxes are publicly available so for simplicity we will store them as host variables in plaintext. If this was our production environment we could encrypt these credential variables at rest using Ansible&rsquo;s <a href="https://docs.ansible.com/ansible/latest/user_guide/vault.html" title="Ansible Vault">Vault</a> feature.</p>
</blockquote>
<p><strong>For the IOS host:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ mkdir inventories/dev/host_vars/ios-xe-mgmt.cisco.com
cphannan@ubuntu:~/netauto$ nano inventories/dev/host_vars/ios-xe-mgmt.cisco.com/ios-xe-mgmt.cisco.com.yml
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_user</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;root&#34;</span><span class="w">
</span><span class="w"></span><span class="k">ansible_password</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;D_Vay!_10&amp;&#34;</span><span class="w">
</span><span class="w"></span><span class="k">ansible_port</span><span class="p">:</span><span class="w"> </span><span class="m">8181</span><span class="w">
</span></code></pre></div><p><strong>For the IOS-XR host:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ mkdir inventories/dev/host_vars/sbx-iosxr-mgmt.cisco.com
cphannan@ubuntu:~/netauto$ nano inventories/dev/host_vars/sbx-iosxr-mgmt.cisco.com/sbx-iosxr-mgmt.cisco.com.yml
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_user</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;admin&#34;</span><span class="w">
</span><span class="w"></span><span class="k">ansible_password</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;C1sco12345&#34;</span><span class="w">
</span><span class="w"></span><span class="k">ansible_port</span><span class="p">:</span><span class="w"> </span><span class="m">8181</span><span class="w">
</span></code></pre></div><p><strong>For the NX-OS host:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ mkdir inventories/dev/host_vars/sbx-nxos-mgmt.cisco.com
cphannan@ubuntu:~/netauto$ nano inventories/dev/host_vars/sbx-nxos-mgmt.cisco.com/sbx-nxos-mgmt.cisco.com.yml
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_user</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;admin&#34;</span><span class="w">
</span><span class="w"></span><span class="k">ansible_password</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Admin_1234!&#34;</span><span class="w">
</span><span class="w"></span><span class="k">ansible_port</span><span class="p">:</span><span class="w"> </span><span class="m">8181</span><span class="w">
</span></code></pre></div><p><strong>For the ACI host:</strong></p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">cphannan@ubuntu:~/netauto$ mkdir inventories/dev/host_vars/sandboxapicdc.cisco.com
cphannan@ubuntu:~/netauto$ nano inventories/dev/host_vars/sandboxapicdc.cisco.com/sandboxapicdc.cisco.com.yml
</code></pre></div><div class="highlight"><pre class="chroma"><code class="language-yaml" data-lang="yaml">---<span class="w">
</span><span class="w">
</span><span class="w"></span><span class="k">ansible_user</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;admin&#34;</span><span class="w">
</span><span class="w"></span><span class="k">ansible_password</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;ciscopsdt&#34;</span><span class="w">
</span></code></pre></div><h3 id="step-8-verify-the-ansible-directory-layout">Step 8: Verify the Ansible directory layout</h3>
<p>The ansible environment&rsquo;s directory structure should now look like this:</p>
<div class="highlight"><pre class="chroma"><code class="language-ini" data-lang="ini"><span class="na">chhannan@ubuntu:~/netauto$ tree </span>
<span class="na">.</span>
<span class="na">├── ansible.cfg</span>
<span class="na">└── inventories</span>
    <span class="na">└── dev</span>
        <span class="na">├── group_vars</span>
        <span class="na">│   ├── aci.yml</span>
        <span class="na">│   ├── iosxr.yml</span>
        <span class="na">│   ├── ios.yml</span>
        <span class="na">│   └── nxos.yml</span>
        <span class="na">├── hosts</span>
        <span class="na">└── host_vars</span>
            <span class="na">├── ios-xe-mgmt.cisco.com</span>
            <span class="na">│   └── ios-xe-mgmt.cisco.com.yml</span>
            <span class="na">├── sandboxapicdc.cisco.com</span>
            <span class="na">│   └── sandboxapicdc.cisco.com.yml</span>
            <span class="na">├── sbx-iosxr-mgmt.cisco.com</span>
            <span class="na">│   └── sbx-iosxr-mgmt.cisco.com.yml</span>
            <span class="na">└── sbx-nxos-mgmt.cisco.com</span>
                <span class="na">└── sbx-nxos-mgmt.cisco.com.yml</span>

<span class="na">8 directories, 10 files</span>
</code></pre></div><h2 id="next-steps">Next Steps</h2>
<p>We now have a solid Ansible directory structure, with the Cisco DevNet sandboxes for multiple OS types defined in a dedicated development inventory.</p>
<p><a href="/posts/2020/06/ansible-and-cisco-devnet-part-2.-writing-a-simple-playbook/">Part 2</a> of this guide will cover creating a simple playbook to retrieve information from the Cisco DevNet sandboxes.</p>
]]></content>
        </item>
        
        <item>
            <title>Cisco ACI - Leaf Switch Replacement</title>
            <link>https://cphannan.github.io/posts/2020/03/cisco-aci-leaf-switch-replacement/</link>
            <pubDate>Fri, 13 Mar 2020 00:00:00 +0000</pubDate>
            
            <guid>https://cphannan.github.io/posts/2020/03/cisco-aci-leaf-switch-replacement/</guid>
            <description>Note: Before following any of the steps below, please take note of the POD ID, Node ID and Node Name of the switch to be replaced. When you unbox the new switch, also note down the serial number of the new switch.
 Power on the new switch and connect a console cable to check if it is running in ACI mode or NX-OS mode. If it is running in NX-OS mode, follow the steps documented in Cisco ACI - Converting NX-OS to ACI Boot Mode to convert the switch to ACI mode.</description>
            <content type="html"><![CDATA[<blockquote>
<p><strong><em>Note:</em></strong> <em>Before following any of the steps below, please take note of the <strong>POD ID</strong>, <strong>Node ID</strong> and <strong>Node Name</strong> of the switch to be replaced. When you unbox the new switch, also note down the <strong>serial number</strong> of the new switch.</em></p>
</blockquote>
<p>Power on the new switch and connect a console cable to check if it is running in ACI mode or NX-OS mode. If it is running in NX-OS mode, follow the steps documented in <a href="/posts/2020/03/cisco-aci-converting-nx-os-to-aci-boot-mode/">Cisco ACI - Converting NX-OS to ACI Boot Mode</a> to convert the switch to ACI mode.</p>
<h2 id="1-decommission-the-faulty-switch-and-remove-it-from-the-controller">1. Decommission the faulty switch and remove it from the controller</h2>
<ol>
<li>In the <strong>ACI GUI</strong>, navigate to <strong>Fabric &gt; Inventory &gt; Fabric Membership</strong>, identify the switch to be replaced.</li>
<li>Right click on the switch to be replaced and select <strong>Decommission</strong>. The status will show as decommissioned.</li>
<li>Right click on the decommissioned switch and select <strong>Remove from Controller</strong>. The status will show as decommissioning and this process can take up to 10 minutes.</li>
</ol>
<h2 id="2-commission-the-new-switch">2. Commission the new switch</h2>
<blockquote>
<p><strong><em>Note:</em></strong> <em>Make sure that the new leaf switch is connected to all of the spine switches in the fabric.</em></p>
</blockquote>
<ol>
<li>In the <strong>ACI GUI</strong>, navigate to <strong>Fabric &gt; Inventory &gt; Fabric Membership &gt; Nodes Pending Registration</strong> and look for a new switch that doesn&rsquo;t have an IP address or node ID assigned. Confirm it is the new switch by verifying that the serial number matches what you noted earlier.</li>
<li>Right click on the new switch to and select <strong>Register</strong>. Configure the <strong>POD ID</strong>, <strong>Node ID</strong> and <strong>Node Name</strong>. Click <strong>Register</strong>. The status will go from unknown to discovered and the APIC will assign it an IP address and add it to the Registered Nodes tab.</li>
<li>On the <strong>Registered Nodes</strong> tab, right click on the new switch and select <strong>Commission</strong>. The status will go to active.</li>
</ol>
<h2 id="3-verify-the-new-switch-status">3. Verify the new switch status</h2>
<ol>
<li>In the <strong>ACI GUI</strong>, navigate to <strong>Fabric &gt; Inventory &gt; Topology</strong> to verify that the new switch is now part of the topology.</li>
<li><strong>SSH</strong> to the <strong>APIC</strong> and run the command <code>acidiag fnvread</code> to confirm that the new switch shows up as &ldquo;active&rdquo;.</li>
</ol>
<h2 id="4-updating-your-known_hosts-file-on-the-apic">4. Updating your known_hosts file on the APIC</h2>
<p>If you try to SSH to the new leaf switch from the APIC and you have connected to the faulty leaf switch previously via SSH, permission will be denied and you will be presented with warnings about <strong><em>&ldquo;POSSIBLE DNS SPOOFING&rdquo;</em></strong> and <strong><em>&ldquo;REMOTE HOST IDENTIFICATION HAS CHANGED&rdquo;</em></strong>.</p>
<p>This is because the RSA host key has changed for the replacement leaf switch and does not match the RSA host key in <em>known_hosts</em>. To fix this you need to remove the RSA host key from your <em>known_hosts</em> file on the APIC as follows:</p>
<div class="highlight"><pre class="chroma"><code class="language-bash" data-lang="bash">apic1# bash
cph-local@apic1:~&gt; ssh-keygen -R &lt;leaf-switch-hostname&gt;
<span class="c1"># Host &lt;leaf-switch-hostname&gt; found: line 2</span>
/home/cph-local/.ssh/known_hosts updated.
Original contents retained as /home/cph-local/.ssh/known_hosts.old
cph-local@apic1:~&gt; <span class="nb">exit</span>
<span class="nb">exit</span>
apic1# ssh &lt;leaf-switch-hostname&gt;
Warning: Permanently added <span class="s1">&#39;&lt;leaf-switch-hostname&gt;,100.64.64.64&#39;</span> <span class="o">(</span>RSA<span class="o">)</span> to the list of known hosts.
</code></pre></div>]]></content>
        </item>
        
        <item>
            <title>Cisco ACI - Converting NX-OS to ACI Boot Mode</title>
            <link>https://cphannan.github.io/posts/2020/03/cisco-aci-converting-nx-os-to-aci-boot-mode/</link>
            <pubDate>Thu, 05 Mar 2020 00:00:00 +0000</pubDate>
            
            <guid>https://cphannan.github.io/posts/2020/03/cisco-aci-converting-nx-os-to-aci-boot-mode/</guid>
            <description>Follow the steps below to convert a Cisco Nexus 9000 Series switch from NX-OS standalone mode to ACI boot mode.
1. Verify which mode the switch is running (ACI or NX-OS) Power on the new switch and connect a console to check if it is running in ACI mode or NX-OS mode. Cisco NX-OS filenames begin with &amp;ldquo;n9000&amp;rdquo; while ACI filenames begin with &amp;ldquo;aci-n9000&amp;rdquo;:
switch# show version 2. Uploading the ACI image The ACI image can be copied to the switch directly via USB or via SCP from the APIC or an SCP server.</description>
            <content type="html"><![CDATA[<p>Follow the steps below to convert a Cisco Nexus 9000 Series switch from NX-OS standalone mode to ACI boot mode.</p>
<h2 id="1-verify-which-mode-the-switch-is-running-aci-or-nx-os">1. Verify which mode the switch is running (ACI or NX-OS)</h2>
<p>Power on the new switch and connect a console to check if it is running in ACI mode or NX-OS mode. Cisco NX-OS filenames begin with &ldquo;n9000&rdquo; while ACI filenames begin with &ldquo;aci-n9000&rdquo;:</p>
<pre><code>switch# show version
</code></pre><h2 id="2-uploading-the-aci-image">2. Uploading the ACI image</h2>
<p>The ACI image can be copied to the switch directly via USB or via SCP from the APIC or an SCP server.</p>
<h4 id="method-1-using-usb">Method 1: Using USB</h4>
<h5 id="copy-the-image-from-the-usb-drive-to-bootflash">Copy the image from the USB drive to bootflash:</h5>
<pre><code>switch# dir usb1:
switch# copy usb1:&lt;aci-image.bin&gt; bootflash:
</code></pre><h4 id="method-2-using-scp-from-the-apic">Method 2: Using SCP from the APIC</h4>
<h5 id="configure-management-connectivity-to-the-apic">Configure management connectivity to the APIC:</h5>
<p>From the <strong>APIC CLI</strong>, view the ACI Out-of-band information and find a spare IP in the management network to use on the new switch:</p>
<pre><code>apic1# show oob-mgmt
</code></pre><p>Set the IP address on the mgmt0 interface of the new switch to allow management connectivity between the new switch and the APIC:</p>
<p><strong>Example:</strong></p>
<pre><code>switch(config)# interface mgmt0

switch(config-if)# ip address &lt;ip-address/length&gt;
</code></pre><p>Patch interface mgmt0 on the new switch into the management network. Confirm connectivity from the new switch to the APIC:</p>
<pre><code>switch# ping &lt;apic-ip-address&gt;
</code></pre><h5 id="copy-the-aci-image-from-the-apic-to-the-switch">Copy the ACI image from the APIC to the switch:</h5>
<p>Enable SCP services on the switch:</p>
<pre><code>switch(config)# feature scp-server
</code></pre><p>From the <strong>APIC CLI</strong>, view and take note of the currently installed leaf/spine switch firmware versions:</p>
<pre><code>apic1# show versions
</code></pre><p>View the firmware images present in the APIC repository and find the &ldquo;aci&rdquo; version of the above switch firmware. Take note of the MD5 checksum:</p>
<pre><code>apic1# show firmware repository detail
</code></pre><p>Use <strong>SCP</strong> to copy the firmware image from the APIC repository to the active supervisor module on the switch:</p>
<pre><code>apic1# scp -r /firmware/fwrepos/fwrepo/&lt;aci-image.bin&gt; admin@&lt;switch-ip-address&gt;:&lt;aci-image.bin&gt;
</code></pre><hr>
<blockquote>
<p><strong><em>Note:</em></strong> <em>For dual-supervisor systems, copy the ACI image from the active to the standby supervisor module.
<strong>Example:</strong> <code>switch(config)# copy bootflash:&lt;aci-image.bin&gt; bootflash://sup-standby/</code></em></p>
</blockquote>
<hr>
<h2 id="3-boot-the-switch-to-the-aci-image">3. Boot the switch to the ACI image</h2>
<p>Configure the switch to not boot from Cisco NX-OS:</p>
<pre><code>switch(config)# no boot nxos
</code></pre><p>Save the configuration:</p>
<pre><code>switch(config)# copy running-config startup-config
</code></pre><blockquote>
<p><strong><em>Note:</em></strong> <em>You must run the copy running-config startup-config command prior to booting the ACI image. Do not run it after you enter the boot aci command.</em></p>
</blockquote>
<p>Boot the switch with the ACI image:</p>
<pre><code>switch(config)# boot aci bootflash:&lt;aci-image.bin&gt;
</code></pre><blockquote>
<p><strong><em>Caution:</em></strong> <em>Do not enter the copy running-config startup-config command after the boot aci command. If you do, the switch will go to the loader&gt; prompt.</em></p>
</blockquote>
<p>Verify the integrity of the file by displaying the MD5 checksum and comparing it to that noted in Step 3:</p>
<pre><code>switch(config)# show file bootflash:&lt;aci-image.bin&gt; md5sum
</code></pre><p>Check the output of the <strong>show boot</strong> command to see if the bootvar is correctly set:</p>
<pre><code>switch# show boot
</code></pre><p>Reload the switch:</p>
<pre><code>switch# reload
</code></pre><blockquote>
<p><strong><em>Note:</em></strong> <em>This reload and conversion can take up to 30 minutes to complete.</em></p>
</blockquote>
<p>Log in to the switch as an administrator to verify it is now running in ACI mode. Notice that the fabric discovery process has been started at this point. It may be some minutes before the services start and we are able to access the switch via the console.:</p>
<p><strong>Example:</strong></p>
<pre><code>User Access Verification
(none) login: admin
*****************************************************************************
    Fabric discovery in progress, show commands are not fully functional
    Logout and Login after discovery to continue to use show commands.
*****************************************************************************
</code></pre><p>You can now log in to the APIC and follow the steps documented in <a href="/posts/2020/03/cisco-aci-leaf-switch-replacement/">Cisco ACI - Leaf Switch Replacement</a> to register and commission the new switch in the ACI Fabric.</p>
<hr>
<blockquote>
<p><strong><em>Note:</em></strong> <em>When commissioned, the new switch may show a degraded health and list faults for a firmware version mismatch of both the BIOS and FPGA firmware. This is because a conversion to ACI basically just boots the switch into the ACI image and, since no install scripts are acting on the box, all the auxiliary steps (that would be performed during a normal upgrade) are not done.</em></p>
<p><em>Normally these faults do not pose an issue and the BIOS and FPGA will be upgraded with the next ACI upgrade administered from the APIC. Alternatively, the BIOS and FPGA firmware can be manually upgraded as follows:</em></p>
<p><strong>FPGA Upgrade (requires a reload)</strong>
<code>/bin/check-fpga.sh FpGaDoWnGrAdE</code>
<code>/usr/sbin/chassis-power-cycle.sh</code></p>
<p><strong>BIOS Upgrade (requires Cisco TAC)</strong>
<em>For the manual upgrade of the BIOS you will need to raise a service request with Cisco so that one of their TAC engineers can assist via a Webex session. There&rsquo;s an enhancement filed to improve this to make the upgrade automatic, however, it&rsquo;s not implemented yet (CSCvm40571).</em></p>
<p><strong>Workaround</strong>
<em>Another option to address this issue is to convert the new switch to an ACI firmware version that is N -1. The switch can then be added to the Fabric and auto-upgraded from the APIC to the correct ACI, BIOS and FPGA versions.</em></p>
</blockquote>
]]></content>
        </item>
        
    </channel>
</rss>
